import styled from "styled-components";
import {colors} from "styles/GlobalStyle"

export const Botao = styled.button
   color: ${colors.while};
   font-weight: bold;
   backgrround-color: ${colors.third};
   pending: 0.6rem;
   border: 0;
   border: 0;
   border-radius: 0.6 rem;
   width: 5rem;
   margin-left: 0.5rem;
